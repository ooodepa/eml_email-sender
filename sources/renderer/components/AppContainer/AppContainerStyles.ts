interface Styles {
  [key: string]: React.CSSProperties;
}

const styles: Styles = {
  wrapper: {
    height: '100vh',
    overflowY: 'scroll',
    overflowX: 'hidden',
    // backgroundColor: 'green',
  },
};

export default styles;
